import React, { useState, useEffect } from 'react';

const PreliminaryDiagnosis = (appointment) => {
  const [step, setStep] = useState(0);
  switch (step) {
    case 1:
      break;

    default:
      break;
  }
};

export default PreliminaryDiagnosis;
